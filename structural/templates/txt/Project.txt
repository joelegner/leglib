===============================================================================
{{ "Project: %-35s %33s" | format(m.name, "Number: %s" % m.number) }}
{{ "%78s" | format(m.timestamp()) }}
===============================================================================
{% for calc in m.calcs.values() %}-------------------------------------------------------------------------------
{{ "%s - %s" | format(calc.name, calc.title) |center(80)}}
-------------------------------------------------------------------------------
{{calc.render()}}{% endfor %}
